name: CI
run-name: >-
  CI (${{ github.event_name == 'pull_request' && 'PR' || 'push-to-main' }})
  ${{ github.event.head_commit.message || github.event.pull_request.title }}

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  ci:
    uses: bamiyanapp/dev-standards/.github/workflows/reusable-ci.yml@v2.4.0
    with:
      frontend_dir: frontend
      backend_dir: backend
      node_version: "22"
      enable_e2e_test: true
      enable_duplication_check: true
      enable_mermaid_render: true
      mermaid_doc_paths: "docs/internal-design.md"
    secrets:
      BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
